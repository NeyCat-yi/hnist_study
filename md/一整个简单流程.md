# 手写数字识别

## 导包

```python
import pandas as pd # 处理数据
from sklearn.model_selection import train_test_split # 划分数据集 pip install scikit-learn
import torch
from torch.utils.data import Dataset, DataLoader
import torch.nn as nn
import torch.nn.functional as F
import torch.optim as optim
from torchvision import transforms
import numpy as np
```

## 导入数据

### 读取 CSV 文件

```python
df = pd.read_csv('datasets/origin.csv')
```

### 使用 sklearn 进行拆分

```python
# test_size=0.2  -> 表示测试集占 20%，训练集自动占 80%
# random_state=42 -> 随机种子，确保每次运行代码切分的结果都一样（可换成任意数字）
# shuffle=True   -> 默认就是 True，表示会先随机打乱数据再切分
train_df, test_df = train_test_split(df, test_size=0.2, random_state=42, shuffle=True)
```

## 数据预处理

### 自定义 Dataset 类

```python
class CSVDataset(Dataset):
    def __init__(self, dataframe, target_column=None, transform=None):
        """
        transform: 传入一个 torchvision.transforms 对象
        """
        self.transform = transform
        
        # 1. 准备数据
        if target_column:
            self.X = dataframe.drop(columns=[target_column]).values
            self.y = dataframe[target_column].values
            self.has_target = True
        else:
            self.X = dataframe.values
            self.has_target = False

        # 注意：这里我们保留原始数据格式 (numpy uint8)，不要急着转 Tensor 或除以 255
        # 这一步交给 transform 去做
        self.X = self.X.astype(np.uint8) 

        if self.has_target:
            self.y = torch.tensor(self.y, dtype=torch.long)

    def __len__(self):
        return len(self.X)

    def __getitem__(self, idx):
        # 取出一行数据 (numpy array)
        data = self.X[idx]
        
        # -----------------------------------------------------------
        # 🔥 关键点：为了使用 PyTorch 强大的图像处理库，
        # 我们通常先把平铺的 784 还原成 28x28 的图片形状
        # -----------------------------------------------------------
        # 如果你的数据不是图片，可以跳过 reshape，但 torchvision 通常喜欢 HxWxC 格式
        data = data.reshape(28, 28) 
        
        # 🔥 调用 transform (如果在外部定义了的话)
        if self.transform:
            data = self.transform(data)
            
        if self.has_target:
            return data, self.y[idx]
        else:
            return data
```

### 定义 Transform(统一做数据预处理)

```python
# 定义处理流程
data_transform = transforms.Compose([
    # 1. 自动转 Tensor 并且归一化到 [0, 1] (即自动除以 255)
    transforms.ToTensor(),
    
    # 2. (可选) 标准化：(x - mean) / std
    # MNIST 的经典均值和方差是 0.1307 和 0.3081
    # 加上这行会让模型收敛更快，但不是必须的
    transforms.Normalize(mean=(0.1307,), std=(0.3081,)),
    
    # 3. 因为 ToTensor 把数据变成了 (1, 28, 28) 的 3D 形状
    # 而你的 MLP 输入层需要 (784) 的 1D 形状
    # 所以我们需要把它压扁 (Flatten)
    transforms.Lambda(lambda x: x.view(-1))
])
```



### 实例化 Dataset 与 DataLoader

```python
batch_size = 32
# 实例化 Dataset，把 transform 传进去
train_dataset = CSVDataset(
    train_df, 
    target_column='label', 
    transform=data_transform  # <--- 注入灵魂
)

train_loader = DataLoader(train_dataset, batch_size=batch_size, shuffle=True) # batch_size 自行定义

# --- 验证一下 ---
features, labels = next(iter(train_loader))

print(features.shape) 
# 应该是 [32, 784] -> 成功被压扁回 MLP 需要的格式
print(features.max()) 
# 应该是 1.0 左右 (如果用了 Normalize 可能会大于 1，如果只用 ToTensor 则是 1.0)
print(features.min())

# B. 实例化 DataLoader
# batch_size=2: 每次吐出 2 条数据
# shuffle=True: 每个 epoch 都会重新打乱数据（训练集通常需要 True）

test_dataset = CSVDataset(
    test_df, 
    target_column='label', 
    transform=data_transform  # <--- 注入灵魂
)

test_loader = DataLoader(test_dataset, batch_size=batch_size, shuffle=True) # batch_size 自行定义
```

### 测试

```python
# 测试 DataLoader
features, labels = next(iter(train_loader))

print("特征形状:", features.shape) # 应该是 [32, 784]
print("标签形状:", labels.shape)   # 应该是 [32]
print("测试通过！")

# --- 测试一下 DataLoader ---
print("开始读取 Batch 数据:")
for batch_idx, (features, labels) in enumerate(train_loader):
    print(f"\nBatch {batch_idx + 1}:")
    print(f"特征 Tensor 形状: {features.shape}")
    print(f"标签 Tensor 形状: {labels.shape}")
    print("特征数据:", features)
    print("标签数据:", labels)
```

## 搭建模型

### 一个简单的 MLP

```python
class DigitRecognitionMLP(nn.Module):
    def __init__(self):
        super(DigitRecognitionMLP, self).__init__()
        
        # 1. 输入层 -> 隐藏层 1
        # 输入维度 784 (代表 28x28 的像素点)
        # 输出维度 512 (这是一个经验值，可以调整)
        self.fc1 = nn.Linear(in_features=784, out_features=512)
        
        # 2. 隐藏层 1 -> 隐藏层 2
        self.fc2 = nn.Linear(512, 256)
        
        # 3. 隐藏层 2 -> 输出层
        # 输出维度 10 (代表数字 0-9，共 10 个类别)
        self.fc3 = nn.Linear(256, 10)
        
        # 定义 Dropout 防止过拟合 (可选，但推荐)
        self.dropout = nn.Dropout(0.2)

    def forward(self, x):
        # x 的形状可能是 (batch_size, 1, 28, 28) 或者 (batch_size, 784)
        
        # 1. 展平 (Flatten)
        # 如果输入是图片 (N, 1, 28, 28)，这步把它变成 (N, 784)
        # 如果输入已经是 CSV 读出来的 (N, 784)，这步不会改变数据，很安全
        x = x.view(-1, 784)
        
        # 2. 第一层运算 + 激活函数 (ReLU)
        x = F.relu(self.fc1(x))
        x = self.dropout(x) # 随机丢弃部分神经元，防止死记硬背
        
        # 3. 第二层运算 + 激活函数
        x = F.relu(self.fc2(x))
        x = self.dropout(x)
        
        # 4. 输出层
        # 注意：这里不需要加 Softmax，因为 PyTorch 的 CrossEntropyLoss 自带了 Softmax
        x = self.fc3(x)
        
        return x
```

### 训练代码

```python
# ==========================================
# 1. 配置设备 (CPU 还是 GPU?)
# ==========================================
# 如果有显卡(NVIDIA)，就用 cuda；如果是 Mac M1/M2，可以用 mps；否则用 cpu
device = torch.device("cuda" if torch.cuda.is_available() else "mps" if torch.backends.mps.is_available() else "cpu")
print(f"正在使用的设备: {device}")

# ==========================================
# 2. 准备模型、损失函数、优化器
# ==========================================
# 实例化模型并移动到设备上
model = DigitRecognitionMLP().to(device)

# 定义损失函数 (多分类问题标准配置)
criterion = nn.CrossEntropyLoss()

# 定义优化器 (Adam 是最常用的，学习率设为 0.001)
optimizer = optim.Adam(model.parameters(), lr=0.001)

# ==========================================
# 3. 开始训练循环
# ==========================================
num_epochs = 10  # 训练多少轮 (遍历整个训练集 10 次)

for epoch in range(num_epochs):
    
    # --- 阶段 A: 训练 (Training) ---
    model.train()  # 启用 Dropout 和 BatchNorm
    running_loss = 0.0
    
    for i, (images, labels) in enumerate(train_loader):
        # 1. 把数据移到 GPU/CPU
        images = images.to(device)
        labels = labels.to(device) # 标签必须是 long 类型
        
        # 2. 清空梯度 (防止梯度累加)
        optimizer.zero_grad()
        
        # 3. 前向传播 (Forward pass)
        outputs = model(images)
        
        # 4. 计算损失 (Loss)
        loss = criterion(outputs, labels)
        
        # 5. 反向传播 (Backward pass)
        loss.backward()
        
        # 6. 更新参数 (Update weights)
        optimizer.step()
        
        running_loss += loss.item()
    
    # 计算本轮平均训练损失
    avg_train_loss = running_loss / len(train_loader)

    # --- 阶段 B: 测试/验证 (Validation) ---
    model.eval()  # 关闭 Dropout，保证测试结果稳定
    correct = 0
    total = 0
    
    # torch.no_grad() 表示这部分不计算梯度，节省内存和时间
    with torch.no_grad():
        for images, labels in test_loader:
            images = images.to(device)
            labels = labels.to(device)
            
            outputs = model(images)
            
            # 获取预测结果: max 返回 (最大值, 最大值的索引)
            # 我们只需要索引 (predicted)，它代表预测的数字 (0-9)
            _, predicted = torch.max(outputs.data, 1)
            
            total += labels.size(0)
            correct += (predicted == labels).sum().item()
            
    accuracy = 100 * correct / total
    
    # --- 打印本轮结果 ---
    print(f"Epoch [{epoch+1}/{num_epochs}] "
          f"| 训练 Loss: {avg_train_loss:.4f} "
          f"| 测试准确率: {accuracy:.2f}%")

print("训练完成！")
```

# Titanic

## 导包

```python
import pandas as pd
import numpy as np
import torch
from torch.utils.data import Dataset, DataLoader

```

## 数据理解与核心策略

泰坦尼克号的黄金法则：**“Women and children first” (女士和儿童优先)**。

1. **关键特征**：
   - **Sex (性别)**：女性幸存率极高。
   - **Pclass (船票等级)**：头等舱（富人）幸存率更高。
   - **Age (年龄)**：小孩幸存率高。
   - **SibSp / Parch (亲属)**：独身一人或大家庭可能存活率低，中等家庭存活率高。
2. **数据陷阱**：
   - **Age**：有很多缺失值，必须填充（比如用平均值）。
   - **Cabin**：缺失太多，通常直接丢弃或只保留“是否有客舱”这个信息。
   - **Name**：看起来没用，但可以提取出称呼（Mr, Mrs, Miss, Master），这隐含了社会地位和年龄信息。

## 数据清洗与特征工程

```python
# 1. 读取数据
train_df = pd.read_csv('train.csv')
test_df = pd.read_csv('test.csv')

# 为了统一处理，先合并，处理完再拆开
# test_df 里面没有 'Survived' 列，先补个占位符
test_df['Survived'] = np.nan
all_data = pd.concat([train_df, test_df], ignore_index=True)

# --- 2. 特征工程 (修复版) ---

# A. 提取称呼 (Title) 并映射
all_data['Title'] = all_data['Name'].apply(lambda x: x.split(',')[1].split('.')[0].strip())
title_map = {
    "Mr": "Mr", "Miss": "Miss", "Mrs": "Mrs", "Master": "Master",
    "Dr": "Officer", "Rev": "Officer", "Col": "Officer", "Major": "Officer",
}
# 注意：匹配不到的填为 'Rare'，防止产生 NaN
all_data['Title'] = all_data['Title'].map(title_map).fillna("Rare")

# B. 填充缺失值
all_data['Age'] = all_data.groupby('Title')['Age'].transform(lambda x: x.fillna(x.median()))
all_data['Fare'] = all_data['Fare'].fillna(all_data['Fare'].median())
all_data['Embarked'] = all_data['Embarked'].fillna('S')

# C. 创造家庭特征
all_data['FamilySize'] = all_data['SibSp'] + all_data['Parch'] + 1

# 🔥 修复重点 1：坚决删掉所有原始字符串列 🔥
# Name, Ticket, Cabin 是最容易导致 object 错误的罪魁祸首
cols_to_drop = ['PassengerId', 'Name', 'Ticket', 'Cabin']
all_data = all_data.drop(columns=cols_to_drop)

# D. One-Hot Encoding (把剩下的分类变量转数字)
# Pclass 虽然是数字 1,2,3，但也建议当作分类变量处理
cat_cols = ['Pclass', 'Sex', 'Embarked', 'Title']
all_data = pd.get_dummies(all_data, columns=cat_cols)

# 🔥 修复重点 2：强制类型转换检查 🔥
# 这一步是为了“捉坏蛋”，如果还有字符串混在里面，这里会立刻报错并告诉你哪一列有问题
features_check = all_data.drop(columns=['Survived'])
try:
    # 尝试强制转为 float，如果报错说明还有脏数据
    features_check = features_check.astype(float)
except ValueError as e:
    print("❌ 数据清洗失败！以下列包含了非数字字符，请检查：")
    # 打印出所有非数字的列名
    print(all_data.select_dtypes(include=['object']).columns)
    raise e

# E. 归一化 (Age, Fare, FamilySize)
# 这一步必须在全部转为数字后做
for col in ['Age', 'Fare', 'FamilySize']:
    mean = all_data[col].mean()
    std = all_data[col].std()
    if std != 0:
        all_data[col] = (all_data[col] - mean) / std
    else:
        all_data[col] = 0

# --- 拆分回训练/测试集 ---
train_processed = all_data[all_data['Survived'].notnull()].copy()
test_processed = all_data[all_data['Survived'].isnull()].drop(columns=['Survived']).copy()

# 确保标签是整数
train_processed['Survived'] = train_processed['Survived'].astype(int)
```

## 定义 Dataset 

```python
# --- 3. Dataset 定义 (更加稳健的写法) ---
class TitanicDataset(Dataset):
    def __init__(self, dataframe, target_column=None):
        if target_column:
            # .values 可能会返回 object 类型，必须显式 .astype(float)
            self.X = dataframe.drop(columns=[target_column]).values.astype(np.float32)
            self.y = dataframe[target_column].values.astype(np.int64)
            self.has_target = True
        else:
            self.X = dataframe.values.astype(np.float32)
            self.has_target = False

        # 转为 PyTorch Tensor
        self.X = torch.from_numpy(self.X)
        if self.has_target:
            self.y = torch.from_numpy(self.y)

    def __len__(self):
        return len(self.X)

    def __getitem__(self, idx):
        if self.has_target:
            return self.X[idx], self.y[idx]
        else:
            return self.X[idx]

# --- 实例化测试 ---
train_dataset = TitanicDataset(train_processed, target_column='Survived')
test_dataset = TitanicDataset(test_processed, target_column=None)

train_loader = DataLoader(train_dataset, batch_size=64, shuffle=True)
test_loader = DataLoader(test_dataset, batch_size=64, shuffle=False)
# 打印一下看看是否成功
sample_x, sample_y = train_dataset[0]
print("✅ 数据转换成功！")
print(f"输入特征维度: {len(sample_x)}") # 应该在 20 左右
print(f"第一条数据特征: {sample_x}")
```

## 模型搭建

```python
class TitanicNet(nn.Module):
    def __init__(self, input_size):
        super(TitanicNet, self).__init__()
        # 3层网络足矣
        self.fc1 = nn.Linear(input_size, 64)
        self.relu = nn.ReLU()
        self.dropout = nn.Dropout(0.2) # 防止过拟合
        self.fc2 = nn.Linear(64, 32)
        self.fc3 = nn.Linear(32, 2)    # 输出 0(死) 或 1(生)

    def forward(self, x):
        x = self.relu(self.fc1(x))
        x = self.dropout(x)
        x = self.relu(self.fc2(x))
        x = self.fc3(x)
        return x

# 动态获取输入维度 (根据你特征工程后的列数)
input_dim = train_processed.shape[1] - 1 # 减去 Survived 列
model = TitanicNet(input_dim)
```

## 训练与生成提交文件

```python
device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
model.to(device)
criterion = nn.CrossEntropyLoss()
optimizer = optim.Adam(model.parameters(), lr=0.001)

# --- 训练 ---
epochs = 50
for epoch in range(epochs):
    model.train()
    for features, labels in train_loader:
        features, labels = features.to(device), labels.to(device)
        
        optimizer.zero_grad()
        outputs = model(features)
        loss = criterion(outputs, labels)
        loss.backward()
        optimizer.step()
    
    if (epoch+1) % 10 == 0:
        print(f"Epoch {epoch+1}, Loss: {loss.item():.4f}")

# --- 预测并生成 CSV ---
model.eval()
predictions = []

with torch.no_grad():
    for features in test_loader:
        features = features.to(device)
        outputs = model(features)
        _, predicted = torch.max(outputs, 1)
        predictions.extend(predicted.cpu().numpy())

# 读取原始 test.csv 只是为了拿 PassengerId
original_test = pd.read_csv('test.csv')

submission = pd.DataFrame({
    'PassengerId': original_test['PassengerId'],
    'Survived': predictions
})

submission.to_csv('titanic_submission.csv', index=False)
print("文件已生成：titanic_submission.csv")
```

# Classify_leaves

## 导包

```python
import pandas as pd
import os
from torch.utils.data import Dataset
from PIL import Image
import torch
import torchvision.models as models
import torch.nn as nn
from torchvision import transforms
import torch.optim as optim
from tqdm import tqdm  # <--- 进度条
import torchvision.models as models
```

## 处理标签和划分数据集

```python
# --- 准备配置 ---
CSV_PATH = '/kaggle/input/classify-leaves/train.csv'       # 训练 CSV
IMG_ROOT = '/kaggle/input/classify-leaves/'   # 图片文件夹
BATCH_SIZE = 32

# --- 读取与预处理 (Pandas) ---
df = pd.read_csv(CSV_PATH)

# 建立标签字典 (Class -> Number)
# 必须基于全部数据建立，保证 Train 和 Val 的映射表是一致的
unique_labels = sorted(df['label'].unique())
class_to_num = {label: i for i, label in enumerate(unique_labels)}
num_to_class = {i: label for i, label in enumerate(unique_labels)} # 留着最后预测用

# 新增一列数字标签 'label_num'
df['label_num'] = df['label'].map(class_to_num)

print(f"总数据量: {len(df)}")
print(f"类别数量: {len(unique_labels)}")

# --- 外部切分 (核心步骤) ---
# stratify=df['label'] 保证了训练集和验证集中，各类别的比例是一样的
train_df, val_df = train_test_split(
    df, 
    test_size=0.2, 
    random_state=42, 
    shuffle=True, 
    stratify=df['label'] 
)
print(train_df.shape)
print(val_df.shape)
```



## 定义 Dataset

```python
class LeavesDataset(Dataset):
    def __init__(self, dataframe, file_root, transform=None, mode='train'):
        """
        dataframe: 传入已经切分好的 pd.DataFrame (train_df 或 val_df)
        file_root: 图片文件夹的根目录
        transform: 数据增强/预处理
        mode: 'train', 'valid' (返回 img, label), 或 'test' (只返回 img)
        """
        # 重置索引非常重要！因为切分后的 dataframe 索引是乱的 (比如 3, 10, 5...)
        #如果不重置，__getitem__ 里的 iloc[idx] 会报错
        self.data = dataframe.reset_index(drop=True)
        self.file_root = file_root
        self.transform = transform
        self.mode = mode

    def __len__(self):
        return len(self.data)

    def __getitem__(self, idx):
        # 1. 获取图片路径 (假设第一列是文件名 'image')
        # 你的 csv 列名可能是 'image' 或 'filename'，请根据实际情况修改
        img_name = self.data.iloc[idx]['image'] 
        img_path = os.path.join(self.file_root, img_name)

        # 2. 读取图片 (转为 RGB 防止有些图片是单通道或 CMYK)
        image = Image.open(img_path).convert('RGB')

        # 3. 应用变换
        if self.transform:
            image = self.transform(image)

        # 4. 根据模式返回结果
        if self.mode == 'test':
            return image
        else:
            # 假设我们在外部已经把 label 转换成了数字，存放在 'label_num' 列
            label = self.data.iloc[idx]['label_num']
            return image, torch.tensor(label, dtype=torch.long)
```

## 定义模型

### 下载预训练模型

```python
# 1. 下载预训练模型
model = models.resnet50(pretrained=True)
# 新版 
# New weights with accuracy 80.858%
# model = models.resnet50(weights=ResNet50_Weights.IMAGENET1K_V2)
# 2. 修改最后一层全连接层 (fc)
# ResNet50 的最后一层输入是 2048，我们要输出 176 类
num_ftrs = model.fc.in_features
model.fc = nn.Linear(num_ftrs, 176) 

# 3. 放到 GPU 上
device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
model = model.to(device)
```

### 加载本地模型

```python
# 保存模型 1
# 假设您想保存在 /home/user/my_models/ 目录下
custom_save_path = '/home/user/my_models/my_resnet50_weights.pth'
# 使用 torch.save() 保存状态字典
torch.save(model.state_dict(), custom_save_path)

# 加载模型 1
model = models.resnet50(weights=None)
model.load_state_dict(torch.load('模型地址'))

# 保存模型 2
# 定义自定义的保存路径
custom_save_path_full = '/home/user/my_models/my_resnet50_full_model.pth'
# 保存整个模型对象
torch.save(model, custom_save_path_full)

# 加载模型 2
# 直接加载整个模型对象
loaded_full_model = torch.load(custom_save_path_full)

# 检查第一层权重的均值是否与随机初始化（接近0）不同
conv1_mean = model.conv1.weight.mean().item()
print(f"第一层 conv1.weight 的均值: {conv1_mean:.6f}")
if abs(conv1_mean) > 1e-4:
    print("✅ 验证成功：加载了有意义的预训练权重。")
else:
    print("❌ 验证失败：权重可能未正确加载，或模型仍然是随机初始化状态。")

```



## 数据增强 Transforms

```python
# 训练集增强
train_transforms = transforms.Compose([
    transforms.Resize((224, 224)),
    transforms.RandomHorizontalFlip(),    # 随机水平翻转
    transforms.RandomVerticalFlip(),      # 随机垂直翻转 (树叶朝上朝下都行)
    transforms.ColorJitter(brightness=0.5, contrast=0.5), # 随机改亮度对比度
    transforms.ToTensor(),
    transforms.Normalize([0.485, 0.456, 0.406], [0.229, 0.224, 0.225]) # ImageNet 标准归一化
])

# 验证/测试集 (不做增强，只做标准化)
val_transforms = transforms.Compose([
    transforms.Resize((224, 224)),
    transforms.ToTensor(),
    transforms.Normalize([0.485, 0.456, 0.406], [0.229, 0.224, 0.225])
])
```

## 实例化 Dataset 和 DataLoader

```python
# 传入切分好的 dataframe
train_dataset = LeavesDataset(
    train_df, 
    IMG_ROOT, 
    transform=train_transform, 
    mode='train'
)
val_dataset = LeavesDataset(
    val_df, 
    IMG_ROOT, 
    transform=val_transform, 
    mode='valid'
)

train_loader = DataLoader(
    train_dataset, 
    batch_size=BATCH_SIZE, 
    shuffle=True, 
    num_workers=0
)
# 验证集通常不需要 shuffle
val_loader = DataLoader(
    val_dataset, 
    batch_size=BATCH_SIZE, 
    shuffle=False, 
    num_workers=0
)
```

## 测试一下 DataLoader

```python
# --- 测试一下 DataLoader ---
print("开始读取 Batch 数据:")
for batch_idx, (features, labels) in enumerate(train_loader):
    print(f"\nBatch {batch_idx + 1}:")
    print(f"特征 Tensor 形状: {features.shape}")
    print(f"标签 Tensor 形状: {labels.shape}")
    print("特征数据:", features)
    print("标签数据:", labels)
```



## 训练

```python
# ==========================================
# 1. 配置设备
# ==========================================
# device = torch.device("cuda" if torch.cuda.is_available() else "mps" if torch.backends.mps.is_available() else "cpu")
print(f"正在使用的设备: {device}")

# ==========================================
# 2. 准备模型、损失函数、优化器
# ==========================================
# 假设 model, train_dataloder, val_dataloader 已经在上面定义好了
# model = model.to(device) 

criterion = nn.CrossEntropyLoss()
optimizer = optim.Adam(model.parameters(), lr=0.001)

# ==========================================
# 3. 开始训练循环 (带进度条)
# ==========================================
num_epochs = 10 

for epoch in range(num_epochs):
    
    # --- 阶段 A: 训练 (Training) ---
    model.train()
    running_loss = 0.0
    
    # <--- 2. 这里用 tqdm 包裹 train_dataloder
    # desc 是进度条左边的文字描述
    # loop 是一个新的迭代器，代替原来的 train_dataloder
    loop = tqdm(train_dataloder, desc=f'Epoch [{epoch+1}/{num_epochs}]')
    
    for images, labels in loop:
        images = images.to(device)
        labels = labels.to(device)
        
        optimizer.zero_grad()
        outputs = model(images)
        loss = criterion(outputs, labels)
        loss.backward()
        optimizer.step()
        
        running_loss += loss.item()
        
        # <--- 3. 关键点：实时更新进度条右侧的小字 (显示当前 Batch 的 Loss)
        loop.set_postfix(loss=loss.item())
    
    # 计算本轮平均训练损失
    avg_train_loss = running_loss / len(train_dataloder)

    # --- 阶段 B: 测试/验证 (Validation) ---
    model.eval()
    correct = 0
    total = 0
    
    with torch.no_grad():
        for images, labels in val_dataloader:
            images = images.to(device)
            labels = labels.to(device)
            outputs = model(images)
            _, predicted = torch.max(outputs.data, 1)
            total += labels.size(0)
            correct += (predicted == labels).sum().item()
            
    accuracy = 100 * correct / total
    
    # <--- 4. 一轮结束后，用 print 打印最终的平均结果
    # tqdm 会自动处理换行，不用担心格式乱掉
    print(f"Epoch {epoch+1} 结束 | 平均 Loss: {avg_train_loss:.4f} | 验证集准确率: {accuracy:.2f}%")

print("训练完成！")
```



## 提交

```python
# 伪代码逻辑
model.eval()
predictions = []

for images in test_loader:
    images = images.to(device)
    outputs = model(images)
    # 取概率最大的那个索引
    _, preds = torch.max(outputs, 1)
    predictions.extend(preds.tolist())

# 把数字转回字符串
final_labels = [dataset.num_to_class[p] for p in predictions]

# 保存到 csv
submission_df = pd.read_csv("test.csv")
submission_df['label'] = final_labels
submission_df.to_csv("submission.csv", index=False)
```

## 获得高分

### 第四阶段：如何获得高分 (Pro Tips)

如果跑完上面的流程，你大概能达到 80%-90% 的准确率。想要冲击 Leaderboard 前排（97%+），你需要用到以下技巧：

1. **TTA (Test Time Augmentation)**: 在预测测试集时，不要只预测一次。把测试图片翻转一下预测一次、调亮一点预测一次，最后把这几次预测的结果**取平均**。这通常能提分 1%~2%。
2. **K-Fold Cross Validation (K折交叉验证)**: 不要只把数据切成固定的 8:2。把数据分成 5 份，训练 5 个模型（每个模型用不同的份做验证集），最后让这 5 个模型一起投票。
3. **学习率调整 (Scheduler)**: 使用 `CosineAnnealingLR`。刚开始学习率大一点，后面越来越小，这样模型能收敛得更细致。